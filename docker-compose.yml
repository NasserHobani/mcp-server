services:
  cs-ai-bridge-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cs-ai-bridge-mcp
    restart: unless-stopped
    ports:
      - "8071:8000"
    env_file:
      - .env
